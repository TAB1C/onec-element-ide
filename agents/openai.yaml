interface:
  display_name: "1C:Element IDE"
  short_description: "Edit 1C:Element source locally and upload it through the control panel."
  default_prompt: "Download 1C:Element project source through the control panel, edit files locally, upload the corrected source, verify IDE/Git visibility, publish/update the app, and test the result."
