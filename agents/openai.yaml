interface:
  display_name: "1C:Element IDE"
  short_description: "Work safely in cloud 1C:Element IDE with Git-visible edits."
  default_prompt: "Open a 1C:Element IDE workspace by URL or id, make source changes in the IDE, verify them, commit/sync through IDE Git, publish/update the app, and test the result."
